{
  "name": "clipnest-ingestion",
  "version": "1.0.0",
  "description": "Video processing pipeline for ClipNest",
  "scripts": {
    "deploy": "bash scripts/deploy.sh",
    "logs:router": "supabase functions logs ingest-router --tail",
    "logs:scraper": "supabase functions logs scrape-worker --tail",
    "logs:enrich": "supabase functions logs enrich-worker --tail"
  },
  "devDependencies": {
    "@deno/types": "^0.0.1",
    "typescript": "^5"
  }
}
