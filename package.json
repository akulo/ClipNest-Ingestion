{
  "name": "clipnest-ingestion",
  "version": "1.0.0",
  "description": "Video processing pipeline for ClipNest",
  "scripts": {
    "deploy": "bash supabase/deploy.sh",
    "logs:router": "supabase functions logs ingest-router --tail",
    "logs:enrich": "supabase functions logs enrich-video --tail"
  },
  "devDependencies": {
    "@deno/types": "^0.0.1",
    "typescript": "^5"
  }
}
